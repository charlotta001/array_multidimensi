#include <iostream>
#include "libr.h"
#include <chrono>
#include <thread>


int main(){
     
     char nim[100];
    std::cin.get(nim, 10);
    std::cout << nim;

    int jam;
    std::cout << " masukan"; 
    std::cin >> jam;
    do{

        std::cout << "halll" << std::endl;
    } while(jam != 1);
    
}